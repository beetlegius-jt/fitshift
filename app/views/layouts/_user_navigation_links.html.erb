<% if user_signed_in? %>
  <%= nav_item t('.sign_out'), destroy_user_session_path, data: { turbo_method: :delete } %>
<% else %>
  <%= nav_item t('.register'), new_customer_path %>
  <%= nav_item t('.sign_in'), new_user_session_path %>
<% end %>
