<% flash.each do |type, msg| %>
  <% if type == 'alert' %>
    <%= tag.div msg, class: "alert alert-danger", role: :alert %>
  <% else %>
    <%= tag.div msg, class: "alert alert-primary", role: :alert %>
  <% end %>
<% end %>
